import React from 'react';
import styled from 'styled-components';

const WelcomeText = styled.Text`
  font-size: 14px;
`;

const DashboardScreen = () => {
  return <WelcomeText>Hello from dashboard.</WelcomeText>
};

export default DashboardScreen;